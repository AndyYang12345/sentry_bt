# 敌方单位是否出现在sentry自瞄视野内
# 该消息数据为uint8数组，与发布者正确通信时会发布长度为9的数组
# 数组0索引位置的值表示是否有识别到装甲板，但未识别出单位编号的目标
uint8[] target

# sentry与识别出的敌方单位的距离
# 该消息数据为float64数组，与发布者正确通信时会发布长度为9的数组
# 数组0索引位置的值表示是否有识别到装甲板，但未识别出单位编号的目标的距离
# 数组1-8索引位置的值依次对应敌方1-8号单位的距离
# 未识别出相应目标时会发送0.0作为默认值
float64[] target_distance

# 识别出的敌方单位投影到sentry水平面上后，相对于sentry的极坐标的极径（以识别目标时刻sentry的相机朝向为极轴正方向）
# 该消息数据为float64数组，与发布者正确通信时会发布长度为9的数组
# 数组0索引位置的值表示是否有识别到装甲板，但未识别出单位编号的目标的极径
# 数组1-8索引位置的值依次对应敌方1-8号单位的极径
# 未识别出相应目标时会发送0.0作为默认值
float64[] target_polar_r

# 识别出的敌方单位投影到sentry水平面上后，相对于sentry的极坐标的极角（以识别目标时刻sentry的相机朝向为极轴正方向）
# 该消息数据为float64数组，与发布者正确通信时会发布长度为9的数组
# 数组0索引位置的值表示是否有识别到装甲板，但未识别出单位编号的目标的极角
# 数组1-8索引位置的值依次对应敌方1-8号单位的极角
# 未识别出相应目标时会发送0.0作为默认值
float64[] target_polar_angle