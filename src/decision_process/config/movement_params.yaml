# ============================================================
# 运动控制参数
# ============================================================
decision_process_node:
  ros__parameters:

    # ---------- 旋转速度 ----------
    max_rotate_speed: 3.14    # 最大旋转速度 (rad/s)
    spin_low_speed: 1.0       # 低速小陀螺 (rad/s)
    spin_mid_speed: 2.0       # 中速小陀螺 (rad/s)
    spin_high_speed: 3.0      # 高速小陀螺 (rad/s)
